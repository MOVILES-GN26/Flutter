import 'package:flutter/material.dart';

/// Vista de Favorites
class FavoritesView extends StatelessWidget {
  const FavoritesView({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Hello World - Favorites',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
